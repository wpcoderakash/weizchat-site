# Copy to .deploy.env (gitignored) and fill in. Used by scripts/deploy.sh.
# Authentication is your SSH key — no password belongs in this file.
DEPLOY_HOST=YOUR_SERVER_IP_OR_HOSTNAME
DEPLOY_USER=weiz-chat
DEPLOY_PATH=/home/weiz-chat/htdocs/www.weiz.chat
DEPLOY_PORT=22
