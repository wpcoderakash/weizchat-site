import { makeSolutionPage } from '../../../components/solutions/solution-page';

const { generateMetadata, Page } = makeSolutionPage('ai-sales-agent');
export { generateMetadata };
export default Page;
