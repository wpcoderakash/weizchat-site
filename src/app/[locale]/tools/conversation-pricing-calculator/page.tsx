import { makeToolPage } from '../../../../components/tools/tool-page';

const { generateMetadata, Page } = makeToolPage('conversation-pricing-calculator');
export { generateMetadata };
export default Page;
