import { makeArticleIndex } from '../../../components/articles/article-list';

const { generateMetadata, Page } = makeArticleIndex('information-center', 'informationCenter');
export { generateMetadata };
export default Page;
